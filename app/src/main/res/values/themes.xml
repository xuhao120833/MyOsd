<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Osd" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Osd" parent="Base.Theme.Osd" >
    </style>

    <style name="SeekBarNoThumb" parent="@android:style/Widget.SeekBar">
        <item name="android:thumb">@drawable/transparent_drawable</item>
    </style>

    <style name="BlurDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowBackgroundBlurRadius">20dp</item> <!-- 设置背景模糊处理半径 -->
        <item name="android:windowIsTranslucent">true</item> <!-- 将窗口设置为半透明 -->
    </style>

</resources>